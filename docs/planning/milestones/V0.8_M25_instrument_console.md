# Milestone M25 — Per-instrument device console

**Closes:** v0.8.0  **Branch:** `0.8-dev`  **Theme:** the device-management
functions live **inside each instrument's own entry**, class-gated. e-Callisto
(heterodyne) exposes its full Callisto toolset; SDR shows the applicable subset.

## Deliverables
- Instrument **capabilities** helper + `GET /api/v1/instruments/{id}/capabilities`
  (bench? overview? class).
- `/portal/instruments/{id}` detail page: status (state/last-file/program/schedule)
  + actions (record/stop/overview/diagnose/reconnect/live) + **class-gated**
  panels: bench detector + noise figure (heterodyne only).
- Link from the instruments list (console) to the detail page.

## Acceptance
- Heterodyne detail shows bench + NF; SDR detail hides them; actions reuse the
  existing endpoints. Gate green; v0.8.0.
