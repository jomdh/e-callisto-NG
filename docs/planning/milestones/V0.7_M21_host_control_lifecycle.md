# Milestone M21 — Host control & lifecycle

**Closes:** v0.7.1  **Branch:** `0.7-dev`  **Theme:** the host-level operations
the design defers behind least-privilege hooks (F12, F15) + the cross-process
recorder status that makes acquisition isolation seamless (F14).

## Deliverables
| # | Origin | Deliverable |
| -- | -- | -- |
| D1 | F14 / ADR-0007 | **DB-backed recorder run-state**: recorder writes state/last-file to the DB; the web app reads it, so it reflects the acquisition daemon's jobs cross-process |
| D2 | DESIGN 8.4 (F12) | **journald log viewer** (read-only tail via a least-priv hook), **receiver reconnect**, guarded **reboot/shutdown** (admin + confirm + documented hook) |
| D3 | DESIGN 15 (F15) | **update apply/rollback runner** behind a least-priv hook + an update *channel* check against a real source |
| D4 | — | a host-action audit trail (reboot/update/reconnect -> AuditEvent) |

## Notes
- All host actions go through a documented, least-privilege hook script (no
  arbitrary command exec from the web process). ADR for the hook contract.

## Acceptance
- Web app shows recorder state even when the `acquire` daemon owns the loops.
- Log viewer/reconnect/reboot/update work against a faked hook in tests; real
  hook documented. Gate green; v0.7.1.
