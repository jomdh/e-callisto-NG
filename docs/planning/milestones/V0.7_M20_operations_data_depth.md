# Milestone M20 — Operations cockpit & data depth

**Closes:** v0.7.0  **Branch:** `0.7-dev`  **Theme:** the §8.1 read-heavy
operational cockpit + richer data browsing (F13).

## Deliverables
| # | DESIGN §8.1 | Deliverable |
| -- | -- | -- |
| D1 | Overview dashboard | per-instrument cards: recording state, current program, **next scheduled action**, last upload, disk/clock; quick start/stop/overview |
| D2 | Live mini-waterfall | a small live waterfall per instrument card (reuse the WS hub) |
| D3 | Data browser depth | **calendar/heatmap** of recordings by day, **in-browser FITS viewer** (render the spectrogram from the FITS), **bulk** download/delete/re-queue |
| D4 | Distribution (operational) | upload queue/history/failures view with per-file status across targets, manual "send now" |

## Acceptance
- Dashboard shows live per-instrument state + next action; mini-waterfalls update.
- Data browser: calendar view, in-browser FITS render, bulk select + actions.
- Gate green; v0.7.0.

## Out of scope
Host ops (M21); GPS/PPS (M22).
