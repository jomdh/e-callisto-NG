# Milestone M19 -- Visual redesign (doncel look)

**Closes:** v0.6.0  **Branch:** `0.6-dev`  **Theme:** adopt doncel.dev's visual
language -- a left vertical sidebar with section icons + the shared M3 design
system -- across the portal. Layout/structure parity with doncel; colours from
our Nebula/Supernova tokens.

## Deliverables
- **S044 shell:** updated `material-design-system.css` (shared asset); new
  `portal.css` sidebar layout; `base.html` left-sidebar shell + `_sidebar.html`
  (icons per section, grouped with labels/dividers, theme toggle, footer,
  collapse + mobile drawer); `sidebar.js`; migrate all pages off the old topbar.
- **S045 components:** buttons -> M3 variants (filled/tonal/text), card grid,
  page headers, dashboard polish.

## Acceptance
- Sidebar on every authed page with active highlighting; none pre-auth.
- Material Icons per section; theme toggle (nebula/supernova); collapse + mobile.
- Gate green; v0.6.0.
