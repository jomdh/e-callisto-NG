# Milestone M22 — Timing precision (GPS/PPS)

**Closes:** v0.7.2  **Branch:** `0.7-dev`  **Theme:** sub-millisecond absolute
timing for stations with a GPS/PPS source (F3, DESIGN 12a).

## Deliverables
| # | Deliverable |
| -- | -- |
| D1 | A **TimeSource** contract (system-NTP / GPS-PPS) + a fake; the recorder timestamps via the active source |
| D2 | GPS/PPS source: read offset/lock from gpsd/chrony refclock (thin, faked in tests); expose lock + offset |
| D3 | Per-class **timestamping model** (DESIGN 12a): heterodyne vs SDR latency compensation as a documented, tested correction |
| D4 | Time page: source, lock, offset history, enforcement policy (extends the M16 drift gate) |

## Notes
- Hardware-dependent; built against a fake TimeSource so the whole model is
  tested without a GPS. ADR for the TimeSource contract.

## Acceptance
- TimeSource contract + system/GPS implementations + fake; timestamps use the
  active source; drift/offset surfaced. Gate green; v0.7.2.
