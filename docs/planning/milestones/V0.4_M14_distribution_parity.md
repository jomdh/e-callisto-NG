# Milestone M14 — Distribution & generator parity

**Closes:** v0.4.3 / v0.4 version close  **Branch:** `0.4-dev`  **Theme:** close the
remaining legacy gaps in upload + frequency-program generation, then merge to
`main`. Maps `WINDOWS_FUNCTIONALITY.md` §2 (GenFrqPrg) and §3 (uploaders).

## Parity gaps -> deliverables

| # | Legacy behavior (evidence) | NG today | Deliverable |
| -- | -- | -- | -- |
| D1 | **SFTP** upload (`UploadPython.py`, `ftpupload.pl`) | local + FTP | `SftpTransport` behind the UploadTransport contract + fake |
| D2 | **Dated backup tree**: move uploaded files into `FITbackup/YYYY/MM/DD/` (`ftp-gz.pl`) | prune only | optional archive-move mode (alt to retention prune) |
| D3 | **GenFrqPrg**: up/down-converter **LO math** + **RFI-exclusion band** when selecting quiet channels (`Unit1.cpp:SelectChannels`) | quiet/even selection, no LO/RFI band | LO conversion + RFI-exclusion band in `freqgen` + the generator UI |
| D4 | Installer "test the connection" affordance (design §8.4) | none | upload-target connection-test endpoint + UI button |

## Acceptance

- Files upload over SFTP (fake-SFTP test).
- An archive-move target lands files under `FITbackup/YYYY/MM/DD/`.
- The generator excludes an RFI band and applies LO conversion (pure tests).
- "Test connection" reports reachability per target.
- Quality gate green; SNR clean; tag v0.4.3; merge `0.4-dev` -> `main`.

## Out of scope

`astro` planning aid, GPS/PPS timing (F3), full wizard -- backlog.
