# Milestone M26 — Recorder/output accuracy (byte-exact)

**Closes:** v0.8.1  **Theme:** fix the recorder/FITS inaccuracies (PARITY_AUDIT A).

## Deliverables
- A1 band-select `<=` (171/450); A2 `F0` `%05.1f` + `.3f` precision; A3 BUNIT
  `45*log(sfu+10)`/`40*log(Tant)` (legacy output mode); A4 10-bit chargepump-on
  control byte; A5 firmware default-continue (10-bit/if_init 37.70); A6 FITS
  binary-table `8.3` TFORM + TSCAL/TZERO; A7 DATAMIN/DATAMAX over written image.

## Acceptance
- Byte-level checks vs legacy formulas; KEEP items (midnight rollover, ]-ack)
  documented untouched. Gate green; v0.8.1.
