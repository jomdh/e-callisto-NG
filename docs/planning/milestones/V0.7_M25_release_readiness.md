# Milestone M25 — Release readiness (AGPLv3)

**Closes:** v0.7.5 (v1.0 candidate)  **Branch:** `0.7-dev`  **Theme:** apply the
AGPLv3 decision (ADR-0003) + release hygiene, so the suite can ship publicly.

## Deliverables
| # | Deliverable |
| -- | -- |
| D1 | `LICENSE` = AGPLv3 text; `pyproject` license metadata = `AGPL-3.0-or-later` |
| D2 | **SPDX header** (`SPDX-License-Identifier: AGPL-3.0-or-later`) on source files |
| D3 | **Plugin-governance** doc (§5b): the contract/process boundary permits independently-licensed (incl. closed) drivers; semver contract policy; **DCO** for contributions (sign-off) |
| D4 | README license section + a short SECURITY.md + CHANGELOG tidy; verify no secrets/keys in the tree; tag a **v1.0 candidate** |

## Acceptance
- LICENSE + SPDX headers present; governance + DCO documented; release hygiene
  checks pass. Gate green; v0.7.5 (v1.0 candidate).

## Notes
- The plugin boundary (ADR-0005/0007 process + versioned contract) is what makes
  AGPL core + closed drivers coexist — call this out explicitly in governance.
