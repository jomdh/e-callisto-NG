# Milestone M30 — Viewer & publication fidelity (v0.8 close)

**Closes:** v0.8.5 / version close  **Theme:** PARITY_AUDIT D6-D7.

## Deliverables
- D6 viewer OVS-specific mV->dB (`/25.4`) + unit labels (mV/ADU/dB) + typed
  Y-axis zoom; D7 wwwgeni PNG: SFU/dB unit selection + ADU->dB calibration + SFU
  `[-10,50]` clamp + y-unit label + 24h UT axis (dated caption) + dated-archive
  copy + `Lightcurves<title>.png` naming.

## Acceptance
- Viewer renders OVS in dB correctly; LC PNG matches wwwgeni layout/naming. Gate
  green; v0.8.5; merge 0.8-dev -> main.
