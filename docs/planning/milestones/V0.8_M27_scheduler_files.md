# Milestone M27 — Scheduler & file-format fidelity

**Closes:** v0.8.2  **Theme:** PARITY_AUDIT B.

## Deliverables
- B1 faithful scheduler.cfg import (mode chars 0-9/A-Z, the 4th `fprog`
  program-switch column, multi-entry not collapsed) + export with fprog/overview
  lines; B2 horizon trim (`delay=horizon/15h` from `Station.horizon_deg`); B3 sun
  rise/set at h0=-0.8333; B4 OVS legacy filename/header/amplitude-gate; B5 LC
  fixed-10-column + Time_UT + version/pwm trailer; B6 SchedulerGeni-equivalent
  scheduler.cfg generator (sunrise/transit/sunset/+0.5h overview).

## Acceptance
- Round-trip a legacy scheduler.cfg without loss; OVS/LC byte-match legacy.
  Gate green; v0.8.2.
