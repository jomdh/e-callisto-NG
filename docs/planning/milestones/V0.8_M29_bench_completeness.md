# Milestone M29 — Bench completeness

**Closes:** v0.8.4  **Theme:** PARITY_AUDIT C.

## Deliverables
- C1 F0 `.3f`; C2 `%5`/`%2` format select + NF `$CRX:<f>,<mV>` parse path; C3
  integration averaging (N reads/point); C4 scalar config `gradient` option for
  NF/bandpass (keep per-point optional); C5 AGC LPF + linearity sweeps
  (PWM->`U2`/`AGC gain=` voltage); C6 time-domain scope/Digitizer (continuous A0
  + trigger threshold + CSV log); C7 relay settle delay + config cold/warm/hot.

## Acceptance
- AGC sweeps + scope endpoints work against the fake; NF matches legacy with the
  scalar gradient. Gate green; v0.8.4.
